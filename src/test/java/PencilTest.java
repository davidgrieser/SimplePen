import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class PencilTest {
    @Test
    public void isSharpened() {
//        Pencil pencil = new Pencil();
//        assertEquals(pencil.isSharpened(), true);
    }
}
